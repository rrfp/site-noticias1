<!DOCTYPE html>
<html lang="pt-BR" data-theme="<%= theme %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verificação MFA</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    /* Variáveis de tema */
    :root {
      --bg-page: #f5f5f5;
      --text-color: #000;
      --card-bg: #fff;
      --input-bg: #fff;
      --input-border: #ccc;
      --brand-color: #007bff;
      --button-text: #fff;
      --card-shadow: rgba(0,0,0,0.25);
    }
    html[data-theme="dark"] {
      --bg-page: #0f0f0f;
      --text-color: #f1f1f1;
      --card-bg: #1a1a1a;
      --input-bg: #222;
      --input-border: #555;
      --brand-color: #4da3ff;
      --button-text: #fff;
      --card-shadow: rgba(0,0,0,0.6);
    }

    body {
      margin: 0;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: var(--bg-page);
      font-family: 'Segoe UI', Tahoma, sans-serif;
      transition: background 0.3s;
    }

    .form-container {
      width: 360px;
      padding: 30px;
      border-radius: 12px;
      background: var(--card-bg);
      box-shadow: 0 8px 24px var(--card-shadow);
      display: flex;
      flex-direction: column;
      gap: 15px;
      text-align: center;
    }

    h2 { color: var(--brand-color); font-size: 22px; margin-bottom: 20px; }

    p.error { color: #ff4d4d; font-weight: bold; margin-bottom: 8px; }
    p.info { font-size: 14px; color: var(--text-color); margin-bottom: 10px; }

    input {
      width: 100%;
      padding: 12px;
      border-radius: 8px;
      border: 1px solid var(--input-border);
      background: var(--input-bg);
      color: var(--text-color);
      font-size: 15px;
      outline: none;
      transition: 0.2s;
    }
    input:focus { border-color: var(--brand-color); box-shadow: 0 0 6px rgba(0,123,255,0.3); }

    button.btn-primary {
      width: 100%;
      padding: 12px;
      background: var(--brand-color);
      color: var(--button-text);
      border: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 15px;
      cursor: pointer;
      transition: 0.2s;
    }
    button.btn-primary:hover { opacity: 0.9; transform: translateY(-1px); }

    .theme-btn {
      position: fixed;
      top: 20px;
      right: 20px;
      background: var(--brand-color);
      color: #fff;
      border: none;
      padding: 10px 14px;
      border-radius: 35px;
      cursor: pointer;
      font-size: 18px;
      box-shadow: 0 4px 10px var(--card-shadow);
      transition: 0.2s;
    }
    .theme-btn:hover { opacity: 0.85; transform: scale(1.05); }

    a { color: var(--brand-color); text-decoration: none; }
    a:hover { text-decoration: underline; }
  </style>
</head>
<body>

<button id="themeToggle" class="theme-btn"><%= theme === "dark" ? "🌙" : "☀️" %></button>

<div class="form-container">
  <h2>Verificação MFA</h2>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <p class="info">Digite o código de 6 dígitos do Google Authenticator para continuar:</p>

  <form action="/auth/mfa/login" method="POST">
    <input type="text" name="token" maxlength="6" placeholder="Código de 6 dígitos" required>
    <button type="submit" class="btn-primary">Confirmar</button>
  </form>

  <p class="info">Não recebeu o código ou quer configurar MFA? <a href="/auth/mfa/setup">Configurar MFA</a></p>
</div>

<script>
  const btn = document.getElementById("themeToggle");
  btn.addEventListener("click", async () => {
    const html = document.documentElement;
    const current = html.getAttribute("data-theme") || "light";
    const next = current === "dark" ? "light" : "dark";
    html.setAttribute("data-theme", next);
    btn.textContent = next === "dark" ? "🌙" : "☀️";
    await fetch("/set-theme", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ theme: next })
    });
  });
</script>

</body>
</html>
